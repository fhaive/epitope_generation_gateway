name: module_2B
channels:
  - bioconda
  - conda-forge
dependencies:
  - python=3.7         # specify the Python version if needed
  - mysqlclient
  - jq
  - pip                # ensure pip is installed in the environment
  - pip:
      - agfusion=1.4.3     # install agfusion with pip
#  - compilers
#  - conda-build                  # needed for some environments where pyensembl downloads require compilation tools
#  - post-build:
#      - pyensembl install --species homo_sapiens --release 95
