name: module_1C
channels:
  - conda-forge
  - bioconda
dependencies:
  - python
  - bcftools
  - pyfaidx
  - configargparse
  - pip                # ensure pip is installed in the environment
  - pip:
      - spladder
      - vcfpy
